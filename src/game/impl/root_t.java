package game.impl;

/**
 * Only thing this interface does is act as the type for everything in the server.  It does not act as a template at all, and therefore has no fields or methods at all.
**/

public interface root_t {};
